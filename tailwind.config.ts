import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      colors: {
        primary: {
          DEFAULT: "#3E0D33",
        },
        secondary: {
          DEFAULT: "#BD8A2E",
        },
      },
    },
  },
};

export default config;
